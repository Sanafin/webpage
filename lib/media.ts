// Optional background video for the hero (e.g. a Seedance loop).
// Drop the file in /public/media and set the paths here; leave as null to use
// the animated gradient backdrop only.
export const heroVideo: { src: string; poster?: string } | null = null
